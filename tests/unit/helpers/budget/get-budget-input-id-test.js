
import { budgetGetBudgetInputId } from 'psm/helpers/get-budget-input-id';
import { module, test } from 'qunit';

module('Unit | Helper | budget/get budget input id');

// Replace this with your real tests.
test('it works', function(assert) {
  const result = budgetGetBudgetInputId('', [42]);
  assert.ok(result);
});

