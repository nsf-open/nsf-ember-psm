import { moduleFor, test } from 'ember-qunit';

moduleFor('service:resolution', 'Unit | Service | resolution', {

});

// Replace this with your real tests.
test('it exists', function(assert) {
  const service = this.subject();
  assert.ok(service);
});
