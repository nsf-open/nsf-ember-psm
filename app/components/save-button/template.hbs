<button class={{btnCSSValue}}
  disabled={{disableButton}}
  title={{title}}
  {{action "onClick"}}>
  {{#if isLoading}}
    <span class="fa fa-spinner fa-spin"></span>
    <span>{{loadingTextFormatted}}</span>
  {{else}}
    {{yield}}
  {{/if}}
</button>